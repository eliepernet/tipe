% ─────────────────────────────────────────────────────────────────────
%  Listing des programmes Python
%  Compilation :
%    lualatex -shell-escape main.tex
%    latexmk  -lualatex -shell-escape main.tex   (recommandé)
% ─────────────────────────────────────────────────────────────────────

\documentclass[a4paper, 11pt]{article}

%── Langue & encodage ─────────────────────────────────────────────────
\usepackage{fontspec}          % gestion des polices (LuaLaTeX natif UTF-8)
\usepackage[french]{babel}
\usepackage{lmodern}

\usepackage[a4paper, top=2cm, bottom=2cm, left=1.5cm, right=1.5cm]{geometry}
\usepackage{parskip}
 
%── Coloration syntaxique Python  (requiert -shell-escape) ────────────
\usepackage{minted}
\setminted[python]{
  breaklines,        % coupure des longues lignes
  fontsize=\footnotesize,
  style=vs,          % mots-clés bleus, commentaires verts (VS light)
}

%── Boîtes (nécessaire pour la variante encadrée) ─────────────────────
\usepackage[most]{tcolorbox}

%── Liens internes ────────────────────────────────────────────────────
\usepackage[hidelinks]{hyperref}

%══════════════════════════════════════════════════════════════════════
%  \fichier{chemin/vers/fichier.py}
%
%  Choisir UNE définition parmi les deux ci-dessous.
%══════════════════════════════════════════════════════════════════════

%── Variante 1 — section simple ───────────────────────────────────────
\newcommand{\fichier}[1]{%
  \section{\texttt{#1}}%
  \inputminted{python}{#1}%
}

%── Variante 2 — encadrée tcolorbox (commenter la variante 1) ─────────
%
% \newcommand{\fichier}[1]{%
%   \addcontentsline{toc}{section}{\texttt{#1}}%
%   \begin{tcolorbox}[
%     enhanced, breakable, arc=3pt,
%     colback=gray!5, colframe=gray!40,
%     title={\texttt{#1}},
%     fonttitle=\small\bfseries, coltitle=black,
%   ]
%     \inputminted{python}{#1}%
%   \end{tcolorbox}%
%   \vspace{0.8em}%
% }

%══════════════════════════════════════════════════════════════════════

\begin{document}

\title{\texttt{Listing des programmes Python}}
\author{}
\date{\today}
\maketitle

% La table des matières liste automatiquement tous les \fichier
\tableofcontents
\newpage

%── Fichiers à inclure ────────────────────────────────────────────────
\fichier{../src/utils.py}
\fichier{../src/models.py}
\fichier{../src/kovasznay.py}
\fichier{../src/kovasznaydeeponet.py}
\fichier{../src/data.py}
\fichier{../src/annaive.py}
\fichier{../src/andata.py}
\fichier{../src/anspeedtransfer.py}

\end{document}